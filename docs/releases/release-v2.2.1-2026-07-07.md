# v2.2.1 发布记录草稿

## 版本信息

- 版本号：`v2.2.1`
- 记录日期：`2026-07-07`
- 状态：本地构建通过，尚未提交、推送、部署

## 本次变更

### 1. 首页 Hero 视觉收口

- Hero 区域采用大图铺满显示，保持官网与客户端共用 Banner 素材的 4:1 横幅方向。
- Hero 图片加载增加区域 URL、原始 URL、`oss.wangzhe.me` / `down.567zm.com` 互换兜底，降低单 CDN 域名异常导致只显示渐变背景的概率。
- 顶部公告从导航下灰条移入 Hero 内容内部，使导航与 Hero 衔接更自然。

### 2. 搜索框宽度调整

- 首页 Hero 搜索框从 `max-w-xl` 收窄到 `max-w-[460px]`。
- 输入右侧留白与搜索按钮宽度同步收紧，避免在全宽 Hero 背景上显得过宽。

## 构建结果

```powershell
cd D:\Ai\gamexxxyx-v2-local
npm.cmd run build
```

结果：构建通过。

## 注意事项

- 当前线上要看到搜索框宽度变化，需要后续提交、推送并部署前端 `dist/`。
- 公告接口 `GET https://api.567zm.com/api/announcements` 当前公开返回 `[]`，代表暂无可见公告；客户端已有占位公告策略，不应再把“看不到真实公告”误判为布局问题。
